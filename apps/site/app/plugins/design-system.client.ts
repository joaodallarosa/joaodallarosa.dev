// Registers the ds-* custom elements. Client-only: Web Components are a DOM/browser
// concern and have no SSR representation (see PROJECT.md §3).
import 'design-system'

export default defineNuxtPlugin(() => {})
